# perf captures: large, binary, and meaningless on another machine
*.perf.data
*.perf.data.old
perf.data
perf.data.old
*.folded

# Cython build products, regenerated by script_nbody.sh setup
*.so
nbody_core.c
build/
__pycache__/
*.pyc

# HDL simulation products, regenerated by hardware/Makefile
hardware/tb_mtf
hardware/stim_*.hex
hardware/stim_params.vh

# copied from the installed benchmark by script_pyflate.sh setup
benchmarks/bm_pyflate_opt*/data/*.tar.bz2

# cloned by the flamegraph stage
FlameGraph/

# local virtualenvs
venv*/

# working notes, not deliverables -- deleted before the final commit
COMMITS.txt
RUN_FROM_SCRATCH.txt
scripts/collect_results.sh
